pdfplumber>=0.11
camelot-py>=1.0
opencv-python-headless>=4.9   # camelot dependency
ghostscript>=0.7              # camelot lattice dependency — also needs the system `gs` binary
openpyxl>=3.1
pyyaml>=6.0

# Optional, for the OCR fallback (not wired up — same as upstream)
# pytesseract>=0.3
# pdf2image>=1.17
